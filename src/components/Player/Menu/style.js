import { flexWidth100 } from 'styles'

const MenuStyle = flexWidth100.extend`
  height: 2rem;
  line-height: 2rem;
  padding: .5rem;
  
  a {
    font-size: 1.5rem;
    display: block;
  }
`;

export default MenuStyle;
